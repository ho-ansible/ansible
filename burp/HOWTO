* Download server .deb:
# wget http://sourceforge.net/projects/urbackup/files/Server/1.4_RC5/debian/stable/urbackup-server_1.4_RC5_i386.deb/download -O urbackup-server_1.4_RC5_i386.deb

* Install deb:
# dpkg -i urbackup-server_1.4_RC5_i386.deb

* Install dependencies:
# apt-get -f install

* Set backup dir to /mnt/urbackup

* Linux client:
update wxGTK to 3.0
./configure && make
make install

# BURP
# apt-get install librsync-dev libz-dev libssl-dev uthash-dev g++ libncurses-dev libacl1-dev
$ git clone git://github.com/grke/burp.git
$ cd burp; ./configure --disable-ipv6 --enable-static
$ make
# make install

# BURP client
# Copy /usr/sbin/burp
# apt-get install librsync1

# upgrade Win64 client:
wget burp-win64-installer-*.exe
rm -fr /cygdrive/c/Program\ Files/Burp/bin-old
mv /cygdrive/c/Program\ Files/Burp/bin /cygdrive/c/Program\ Files/Burp/bin-old
./burp-win64-installer-*.exe /S

* Exclude:
[A-Z]:/Users/[^/]+/AppData/Local/Temp
[A-Z]:/Documents and Settings/[^/]+/Cookies
[A-Z]:/Documents and Settings/[^/]+/Recent
[A-Z]:/Documents and Settings/[^/]+/Local Settings/Temp
[A-Z]:/RECYCLER
[A-Z]:/pagefile.sys
[A-Z]:/swapfile.sys
[A-Z]:/hiberfil.sys

# burp-ui
# apt-get install python-pip python-dev libffi-dev
# pip install burp-ui
# pip install pyOpenSSL
# cp /usr/local/share/burpui/etc/burpui.cfg /etc/burp/

---
# Playbook assigning role to inventory group
- name: include tinc networks
  tags: 
    - vpn
    - tinc
    - "{{ item }}"
  include_role: tinc
  vars: "../inv/.tinc/networks/{{ item }}.yml"
  with_items: tinc_networks


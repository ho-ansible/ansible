[defaults]
inventory = inv
roles_path = roles

retry_files_save_path = .retry
ansible_managed = 'Ansible managed: template last modified %Y-%m-%d %H:%M:%S'
forks = 8

[privilege_escalation]
become = True

[ssh_connection]
pipelining = True

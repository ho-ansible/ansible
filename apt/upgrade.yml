---
- name: Update+Upgrade Packages
  hosts: debian
  become: true
  gather_facts: false

  tasks:

  - name: Update APT cache
    apt: update_cache=yes

  - name: Install package aptitude
    apt: pkg=aptitude state=latest install_recommends=no force=yes

  - name: Upgrade all packages
    apt: upgrade=safe


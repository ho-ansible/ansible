---

- name: Burp | directories
  tags:
  - burp
  - dirs
  file:
    path: "{{item}}"
    state: directory
    mode: 0750
  with_items:
  - /etc/burp
  - /etc/burp/keys
  - /etc/burp/upgrade

- name: Burp | dependencies
  tags:
  - burp
  - pkgs
  apt:
    pkg: librsync-dev
    state: installed

- name: Burp | executables
  tags:
  - burp
  - bin
  copy:
    src: "sbin/{{item}}"
    dest: /usr/sbin/
    mode: 0755
  with_items:
  - burp
  - burp_ca

- name: Burp | config
  tags:
  - burp
  - config
  template:
    src: burp.conf.j2
    dest: /etc/burp/burp.conf
    mode: 0640

- name: Burp | cronjob
  tags:
  - burp
  - cron
  copy:
    src: burp.cron
    dest: /etc/cron.daily/
    mode: 0750

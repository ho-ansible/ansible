---

- name: sudo | packages
  tags:
  - package
  package:
    name: "{{ item }}"
    state: present
  with_items:
  - sudo

- name: sudo | group
  tags:
  - group
  group:
    name: sudo
    state: present

- name: sudo | /etc/sudoers
  tags:
  - config
  lineinfile:
    dest: /etc/sudoers
    regexp: '^%sudo'
    line: "%sudo   ALL=(ALL:ALL) NOPASSWD: ALL"
    state: present

- name: sudo | user
  tags:
  - user
  user:
    name: "{{ ansible_user }}"
    groups: "sudo, adm"

- name: sudo | SSH keys
  tags:
  - ssh
  authorized_key:
    user: "{{ ansible_user }}"
    key: "{{ item }}"
  with_items: "{{ ssh_authorized_keys }}"

---

- name: systemd-resolved | remove resolvconf
  tags:
    - resolver
    - package
  package:
    name: "{{ item }}"
    state: absent
    purge: yes
  with_items:
    - resolvconf
    - openresolv

- name: systemd-resolved | glibc NSS module
  tags:
    - resolver
    - package
  package:
    name: libnss-resolve

- name: systemd-resolved | config
  tags:
    - resolver
    - config
  template:
    src: resolved.conf.j2
    dest: /etc/systemd/resolved.conf
  notify: restart resolved

- name: systemd-resolved | enable
  tags:
    - resolver
  copy:
    src: resolv.conf
    dest: /etc/

- name: systemd-resolved | service
  tags:
    - resolver
    - service
  service:
    name: systemd-resolved
    state: started
    enabled: yes

---

- name: journald | server | config
  tags:
    - config
  template:
    src: journal-remote.conf.j2
    dest: /etc/systemd/journal-remote.conf
  notify: restart journal

- name: journald | server | socket
  tags:
    - service
  service:
    name: systemd-journal-remote.socket
    enabled: yes
